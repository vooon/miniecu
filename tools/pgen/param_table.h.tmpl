/* AUTOGENERATED FILE, DO NOT EDIT
 *
 * Generated ${gen_time}
 * from: ${source_file}
 */

#ifndef PARAM_TABLE_H_INCLUEDED
#define PARAM_TABLE_H_INCLUEDED

#include "param.h"

<%def name="enum_definition(param_id, var_def)">\
enum ${param_id.lower()} {
% for k, v in sorted(var_def.enum.iteritems()):
	${param_id.upper()}_${k} = ${v};
% endfor
}\
</%def>

/** Enums
 * @{
 */
% for k, v in sorted(param_table.parameters_with_enum.iteritems()):
//! Enum for param: ${k}
${enum_definition(k, v)};
%     if not loop.last:

%     endif
% endfor
/** @} */

const uint32_t param_format_version_be32;
const param_entry param_table[];
const size_t param_table_size;

#endif /* PARAM_TABLE_H_INCLUEDED */
